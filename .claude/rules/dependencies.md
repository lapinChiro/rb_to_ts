---
paths:
  - "package.json"
---

# 依存関係のバージョン管理

## トリガー

`package.json` に依存を追加する、または既存の依存を更新するとき。

## アクション

1. 追加・更新する依存の最新バージョンを確認する
2. 最新バージョンを指定する
3. 既存の依存との整合性を確認する (`npm install` + `npx tsc --noEmit` が通ること)
4. 整合性が取れない場合、関連する依存も含めて最新に揃える

## 禁止事項

- 理由なく古いバージョンを指定すること
